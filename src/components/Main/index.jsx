// components
import Form from '../Form/indes';
// styles
import style from './Main.module.scss';

const Main = () => {
	return (
		<div className={style.wrapper}>
			<Form />
		</div>
	);
};

export default Main;
